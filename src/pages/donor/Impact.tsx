import { Leaf, Recycle, TrendingUp, WalletCards } from 'lucide-react';
import AppShell from '../../components/layout/AppShell';
import StatCard from '../../components/common/StatCard';
import { useRaw } from '../../store/RawStore';

export default function Impact() {
  const { impact } = useRaw();
  return <AppShell><div><p className="text-sm font-semibold text-teal-700">Impact</p><h1 className="mt-1 text-3xl font-black tracking-tight">Your circular economy impact</h1><p className="mt-2 text-sm text-slate-500">A transparent view of resources kept in circulation through RAW.</p><div className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><StatCard label="Material recovered" value={`${impact.materialRecovered.toLocaleString()} kg`} icon={Recycle}/><StatCard label="Value recovered" value={`₹${impact.valueRecovered.toLocaleString()}`} icon={WalletCards}/><StatCard label="Successful exchanges" value={`${impact.successfulTransactions}`} icon={TrendingUp}/><StatCard label="Reuse cycles" value={`${impact.reuseCycles}`} icon={Leaf}/></div><div className="mt-6 rounded-2xl border border-slate-200 bg-white p-6"><h2 className="font-bold">Impact ledger</h2><div className="mt-4 grid gap-3 md:grid-cols-3"><div className="rounded-xl bg-slate-50 p-4"><div className="text-xs text-slate-500">Estimated waste avoided</div><div className="mt-1 text-2xl font-black">{impact.estimatedWasteAvoided} kg</div></div><div className="rounded-xl bg-slate-50 p-4"><div className="text-xs text-slate-500">Direct reuse</div><div className="mt-1 text-2xl font-black">68%</div></div><div className="rounded-xl bg-slate-50 p-4"><div className="text-xs text-slate-500">Average trust score</div><div className="mt-1 text-2xl font-black">94</div></div></div><p className="mt-5 text-xs leading-5 text-slate-500">* Environmental outcomes are estimates derived from material and transaction records; methodology can be made configurable in the backend.</p></div><div className="mt-6 grid gap-6 lg:grid-cols-[1.1fr_0.9fr]"><section className="overflow-hidden rounded-2xl border border-slate-200 bg-white"><div className="grid min-h-72 md:grid-cols-[0.9fr_1.1fr]"><div className="p-6 sm:p-8"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-teal-700">Material spotlight</p><h2 className="mt-2 text-2xl font-black tracking-tight">Keep high-value materials moving.</h2><p className="mt-3 text-sm leading-6 text-slate-500">RAW prioritizes direct reuse and recovery so resources stay useful for longer.</p><div className="mt-6 flex flex-wrap gap-2"><span className="rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-semibold text-emerald-800">Direct reuse first</span><span className="rounded-full bg-teal-50 px-3 py-1.5 text-xs font-semibold text-teal-800">Local-first</span></div></div><div className="relative min-h-64 overflow-hidden bg-slate-100"><img src="/raw-materials-dark.svg" alt="Copper, cardboard, electronics and reusable materials" className="h-full w-full object-cover"/><div className="absolute inset-0 bg-gradient-to-r from-slate-950/25 via-transparent to-transparent"/></div></div></section><aside className="overflow-hidden rounded-2xl border border-teal-100 bg-teal-50/70"><img src="/raw-materials-light.svg" alt="Reusable raw material collection" className="h-44 w-full object-cover"/><div className="p-5"><p className="text-xs font-semibold uppercase tracking-[0.18em] text-teal-700">Circular pathways</p><p className="mt-2 text-sm leading-6 text-slate-600">Reuse, repair and repurpose can preserve more value than sending a usable material directly to recycling.</p></div></aside></div></div></AppShell>;
}
