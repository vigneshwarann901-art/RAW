import { Download, FileText, Leaf } from 'lucide-react';
import { Link, useParams } from 'react-router-dom';
import AppShell from '../../components/layout/AppShell';
import Badge from '../../components/common/Badge';
import { useRaw } from '../../store/RawStore';

export default function Receipt() {
  const { id } = useParams();
  const { transactions } = useRaw();
  const tx = transactions.find((item) => item.id === id) ?? transactions[0];
  if (!tx) return <AppShell><div className="rounded-2xl border border-slate-200 bg-white p-8">Receipt not found.</div></AppShell>;

  const download = () => {
    const content = [
      'RAW — E-RECEIPT',
      `Transaction: ${tx.id}`,
      `Date: ${new Date(tx.createdAt).toLocaleString()}`,
      `Material: ${tx.material}`,
      `Quantity: ${tx.quantity} ${tx.unit}`,
      `Agreed price: ₹${tx.agreedPrice.toLocaleString()}`,
      `Total: ₹${tx.total.toLocaleString()}`,
      `Status: ${tx.status}`,
      'Generated by RAW Reusable & Recoverable Network',
    ].join('\n');
    const blob = new Blob([content], { type: 'text/plain;charset=utf-8' });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `${tx.id}-receipt.txt`;
    a.click();
    URL.revokeObjectURL(url);
  };

  const printReceipt = () => window.print();

  return <AppShell><div className="mx-auto max-w-3xl"><div className="flex items-end justify-between gap-4"><div><p className="text-sm font-semibold text-teal-700">Transaction receipt</p><h1 className="mt-1 text-3xl font-black tracking-tight">E-receipt</h1><p className="mt-2 text-sm text-slate-500">A simple, traceable record of the completed exchange.</p></div><div className="flex gap-2 print:hidden"><button onClick={printReceipt} className="rounded-xl border border-slate-200 bg-white px-4 py-3 text-sm font-semibold text-slate-700">Print / Save PDF</button><button onClick={download} className="inline-flex items-center gap-2 rounded-xl bg-slate-950 px-4 py-3 text-sm font-semibold text-white"><Download className="h-4 w-4"/> Download text</button></div></div>
    <section className="mt-7 rounded-3xl border border-slate-200 bg-white p-6 sm:p-8"><div className="flex items-center justify-between border-b border-slate-100 pb-5"><div className="flex items-center gap-3"><div className="grid h-11 w-11 place-items-center rounded-xl bg-slate-950 text-white"><Leaf className="h-5 w-5"/></div><div><div className="font-black tracking-tight">RAW</div><div className="text-xs text-slate-500">Reusable & Recoverable Network</div></div></div><Badge tone={tx.status === 'COMPLETED' ? 'success' : 'brand'}>{tx.status}</Badge></div><div className="mt-7 grid gap-4 sm:grid-cols-2"><div><div className="text-xs text-slate-400">Transaction ID</div><div className="mt-1 font-bold">{tx.id}</div></div><div><div className="text-xs text-slate-400">Issued</div><div className="mt-1 font-bold">{new Date(tx.createdAt).toLocaleString()}</div></div><div><div className="text-xs text-slate-400">Material</div><div className="mt-1 font-bold">{tx.material}</div></div><div><div className="text-xs text-slate-400">Quantity</div><div className="mt-1 font-bold">{tx.quantity} {tx.unit}</div></div><div><div className="text-xs text-slate-400">Agreed price</div><div className="mt-1 font-bold">₹{tx.agreedPrice.toLocaleString()}</div></div><div><div className="text-xs text-slate-400">Total value</div><div className="mt-1 text-xl font-black">₹{tx.total.toLocaleString()}</div></div></div><div className="mt-8 rounded-2xl bg-slate-50 p-5"><div className="flex items-center gap-2 font-semibold"><FileText className="h-4 w-4 text-teal-700"/> Pickup</div><div className="mt-2 text-sm text-slate-600">{new Date(tx.pickupTime).toLocaleString()}</div></div><p className="mt-6 text-xs leading-5 text-slate-500">This receipt records the transaction details stored by the RAW MVP. Environmental metrics shown elsewhere in the app are estimates based on material records.</p></section><Link to="/donor/transactions" className="mt-5 inline-block text-sm font-semibold text-teal-700">← Back to transactions</Link></div></AppShell>;
}
